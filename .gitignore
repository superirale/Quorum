node_modules/
dist/
*.tsbuildinfo
.DS_Store

# Go
bin/
data/

# The operator console's home: secret keys (0600) and the selected workspace.
# Never commit this. `quorum keygen` makes a new one in seconds.
.quorum/

# Agent state: the once() ledger, cursors, counters, lease epochs. Local and
# per-process by design — see packages/sdk/src/store.ts.
state/

# Written by the deploy-agent demo for its verifier to read back. Fresh keys
# every run, so committing one would be committing a specific afternoon.
transcript.json
